"""LLM providers, market commentary and AI signal recovery.

Nothing here touches the broker: core_ai_signal_fallback (which makes a real
bridge.modify_order call) deliberately stays in core/ until the trading phases.
"""
