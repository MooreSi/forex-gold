{
  "_comment": [
    "Whole modules that nothing imports, transitively, from the app entrypoints.",
    "An entry is a recorded debt, NOT an approval to keep the code. The gate",
    "(orphan_modules --check) fails on any orphan NOT listed, and on any listed",
    "module that is no longer an orphan (delete its entry when you delete/wire it).",
    "",
    "The 3 superseded-dead database.py clones were DELETED 2026-08-10. The 4 below",
    "are built-but-UNWIRED (not dead): deleting them would destroy real",
    "functionality and hide a wiring bug. Each needs an OWNER (Simon) wire-vs-remove",
    "decision -- see docs/questions/002-unwired-modules.md. Do NOT delete until then."
  ],
  "allowed": [
    {
      "module": "backend.src.services.broker.fake_bridge",
      "file": "backend/src/services/broker/fake_bridge.py",
      "loc": 316,
      "class": "awaiting-simon-gated-seam",
      "reason": "Debug-mode FakeMT5Bridge (stage2 phase5/010). Fully built and test-covered (tests/services/broker/); production wiring is the 3-line _make_bridge seam in runtime.py, which is Simon-gated (sign-off + demo, local-debug-mode 020). Remove this entry the day the seam lands."
    },
    {
      "module": "backend.src.services.broker.fake_market",
      "file": "backend/src/services/broker/fake_market.py",
      "loc": 124,
      "class": "awaiting-simon-gated-seam",
      "reason": "FakeMT5Bridge's deterministic tick engine -- imported only by fake_bridge.py, so it orphans together with it. Same seam, same removal condition."
    },
    {
      "module": "backend.src.services.breakout_signal.backtest",
      "file": "backend/src/services/breakout_signal/backtest.py",
      "loc": 226,
      "class": "unwired-tool",
      "reason": "Walk-forward harness to validate breakout recalibrations BEFORE they touch live trading; its docstring documents manual invocation (run_walkforward/report). Likely a dev tool, not dead. The frontend backtest page uses a different system (services.backtest)."
    },
    {
      "module": "backend.src.services.test_signal.auth",
      "file": "backend/src/services/test_signal/auth.py",
      "loc": 51,
      "class": "unwired-needs-owner-decision",
      "reason": "Password protection for the TEST module (PBKDF2 + hardware-fingerprint salt). Zero references -- appears disconnected. Owner: intentionally removed, or a dropped wire?"
    },
    {
      "module": "backend.src.services.cluster.remote.ca",
      "file": "backend/src/services/cluster/remote/ca.py",
      "loc": 250,
      "class": "staged-rollout",
      "reason": [
        "The private CA for the licence/admin channel (docs/todo/bugs/014),",
        "landed 2026-09-01 as stage one of three. Nothing in the app imports it",
        "YET, and that is deliberate: enforcing verification before the admin",
        "server has a CA-signed certificate would lock every client -- including",
        "the owner's own admin console -- out of the machine they would need to",
        "fix it from.",
        "",
        "Stage one (this entry): the CA, the issuing tool, and the tests, none of",
        "it wired. Stage two: the client verifies against the bundled CA on the",
        "internet path and TOFU-pins on the LAN path. Stage three: the server",
        "serves the CA-signed certificate, after the owner has generated the",
        "authority and deployed a certificate to the VPS.",
        "",
        "DELETE THIS ENTRY when stage two wires it. Used today by",
        "tools/make_remote_ca.py and tests/remote/test_remote_ca.py."
      ]
    }
  ]
}
