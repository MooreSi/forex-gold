"""Channel policy: per-channel config, trust scorecards, learned parsing rules,
unrecognised-message queue, and the AI that recommends per-channel strategy.

Policy only -- the Telegram transport (reader, alerts, bot) is phase 5.
"""
